spring:
  datasource:
    url: jdbc:mysql://localhost/mydb
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      # 更新或者创建表
      ddl-auto: update

    show-sql: true
